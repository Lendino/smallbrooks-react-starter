# `smallbrooks-react-starter`

* Starter app based on `create-react-app`.
* Using `smallbrooks-react-scripts` instead of `react-scripts` to add less support.
* Includes semantic-ui with possibility for theming.
